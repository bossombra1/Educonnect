import React,{useEffect,useState}from'react';import{ActivityIndicator,StyleSheet,View}from'react-native';import{NavigationContainer}from'@react-navigation/native';import{createNativeStackNavigator}from'@react-navigation/native-stack';import{createBottomTabNavigator}from'@react-navigation/bottom-tabs';import{Bell,Home,MessageCircle,User}from'lucide-react-native';import{Colors}from'@/theme';import authService from'@/services/auth.service';import AuthNavigator from'./AuthNavigator';import HomeScreen from'@/screens/native/HomeScreen';import MessagesScreen from'@/screens/native/MessagesScreen';import NotificationsScreen from'@/screens/native/NotificationsScreen';import ProfileScreen from'@/screens/native/ProfileScreen';import{UnreadProvider}from'./UnreadContext';import{navigationRef}from'./NavigationRef';import type{AppTabParamList,RootStackParamList}from'./types';
const RootStack=createNativeStackNavigator<RootStackParamList>();const AppTabs=createBottomTabNavigator<AppTabParamList>();
function AppNavigator(){return <UnreadProvider><AppTabs.Navigator screenOptions={({route})=>({headerShown:false,tabBarActiveTintColor:Colors.primary,tabBarInactiveTintColor:Colors.gray400,tabBarStyle:{backgroundColor:Colors.white,borderTopColor:Colors.gray100,height:62,paddingTop:6,paddingBottom:8},tabBarIcon:({color,size})=>{const Icon=route.name==='Home'?Home:route.name==='Messages'?MessageCircle:route.name==='Notifications'?Bell:User;return <Icon size={size} color={String(color)}/>;}})}><AppTabs.Screen name="Home" component={HomeScreen} options={{title:'Accueil'}}/><AppTabs.Screen name="Messages" component={MessagesScreen} options={{title:'Messages'}}/><AppTabs.Screen name="Notifications" component={NotificationsScreen} options={{title:'Notifications'}}/><AppTabs.Screen name="Profile" component={ProfileScreen} options={{title:'Profil'}}/></AppTabs.Navigator></UnreadProvider>}
export default function RootNavigator(){const[initialRoute,setInitialRoute]=useState<'Auth'|'App'|null>(null);useEffect(()=>{let mounted=true;authService.getStoredToken().then(token=>{if(mounted)setInitialRoute(token?'App':'Auth');}).catch(()=>{if(mounted)setInitialRoute('Auth');});return()=>{mounted=false;};},[]);if(!initialRoute)return <View style={styles.loading}><ActivityIndicator size="large" color={Colors.primary}/></View>;return <NavigationContainer ref={navigationRef}><RootStack.Navigator initialRouteName={initialRoute} screenOptions={{headerShown:false}}><RootStack.Screen name="Auth" component={AuthNavigator}/><RootStack.Screen name="App" component={AppNavigator}/></RootStack.Navigator></NavigationContainer>}
const styles=StyleSheet.create({loading:{flex:1,alignItems:'center',justifyContent:'center',backgroundColor:Colors.background}});
